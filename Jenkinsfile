pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                 echo 'Starting the build stage.1..'
            }
        }
        stage('deploy') {
            steps {
                 echo 'Starting the deploy stage.1..'
            }
        }
     
    }
}
