pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                 echo 'Starting the build stage...'
                 sh 'npm install'
            }
        }
     
    }
}
